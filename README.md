# MINISHELL
MiniShell is a lightweight, POSIX-inspired command-line shell built from scratch in C. The project demonstrates a deep understanding of operating system concepts by directly interfacing with the Linux kernel through system calls — without relying on high-level abstractions like system().
